name: AI Discovery (Test)

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify Setup
        run: |
          echo "AI Discovery workflow is correctly configured"
          echo "Repository  : ${{ github.repository }}"
          echo "Triggered by: ${{ github.actor }}"
          echo "Ref         : ${{ github.ref }}"
          echo ""
          echo "This is a test run. Replace with ai-discovery.yml when ready for production."
